import { darkTheme } from './theme';

export default `body.dark{background-color:${darkTheme.mainBackgroundColor};color:${darkTheme.textColor};}body.dark button{color:${darkTheme.textColor};border-color:${darkTheme.accentColor};}body.dark a{color:${darkTheme.accentColor};}`;
