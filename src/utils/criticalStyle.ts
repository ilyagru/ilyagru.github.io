import { darkTheme } from './theme';

const criticalStyle = `body.dark{background-color:${darkTheme.mainBackgroundColor};color:${darkTheme.textColor};}body.dark button{color:${darkTheme.textColor};border-color:${darkTheme.accentColor};}body.dark a{color:${darkTheme.accentColor};}`;

export default criticalStyle;
